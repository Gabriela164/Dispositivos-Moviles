<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color de fondo -->
    <solid android:color="#FFFFFF" />

    <!-- Color y grosor del borde -->
    <stroke
        android:width="25dp"
        android:color="#f5eef8" />

    <!-- Bordes redondeados -->
    <corners android:radius="5dp" />
</shape>
